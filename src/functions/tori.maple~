#this is a collection of ten functions, corresponding to tori. They should all accept order, and a twisted weight from the weight twister function

brauer_complex_tone := proc(a,b,c,order)

#if else tree 
if a==b==c==0 then 
   chi11(1)
   chi12(3) 

   	    #and so on

end proc;

brauer_complex_ttwo := proc(a,b,c,order)


end proc;

brauer_complex_tthree := proc(a,b,order)

end proc;



